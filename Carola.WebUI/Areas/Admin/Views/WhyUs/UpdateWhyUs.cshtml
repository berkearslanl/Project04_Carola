@model GetWhyUsByIdDto
@using Carola.DtoLayer.Dtos.WhyUsDtos
@{
    Layout = "~/Views/AdminLayout/Index.cshtml";
}

<style>
    :root { --bg-dark:#0d0f14; --bg-dark2:#13161e; --bg-dark3:#1a1e2a; --accent:#e8ff47; --accent-dim:rgba(232,255,71,.12); --danger:#ff5259; --border:rgba(255,255,255,.07); --text:#e2e8f0; --text-muted:#7a8499; --radius:12px; --radius-lg:18px; }
    *, *::before, *::after { box-sizing:border-box; margin:0; padding:0; }
    .uw-root { padding:28px; font-family:'DM Sans','Segoe UI',sans-serif; color:var(--text); }
    .uw-breadcrumb { display:flex; align-items:center; gap:6px; font-size:12px; color:var(--text-muted); margin-bottom:6px; }
    .uw-breadcrumb a { color:var(--text-muted); text-decoration:none; } .uw-breadcrumb a:hover { color:var(--text); } .uw-breadcrumb span { color:var(--text); }
    .uw-page-header { display:flex; align-items:flex-end; justify-content:space-between; gap:16px; margin-bottom:28px; flex-wrap:wrap; }
    .uw-page-title { font-family:'Syne',Georgia,sans-serif; font-size:26px; font-weight:700; color:var(--text); line-height:1; }
    .uw-page-subtitle { font-size:13px; color:var(--text-muted); margin-top:5px; }
    .btn-accent { background:var(--accent); color:#0d0f14; border:none; border-radius:10px; padding:9px 20px; font-size:13px; font-weight:600; font-family:inherit; cursor:pointer; display:inline-flex; align-items:center; gap:7px; text-decoration:none; transition:opacity .2s; }
    .btn-accent:hover { opacity:.85; color:#0d0f14; }
    .btn-outline { background:transparent; color:var(--text-muted); border:1px solid var(--border); border-radius:10px; padding:9px 18px; font-size:13px; font-weight:500; font-family:inherit; cursor:pointer; display:inline-flex; align-items:center; gap:7px; text-decoration:none; transition:background .2s,color .2s; }
    .btn-outline:hover { background:var(--bg-dark2); color:var(--text); }
    .uw-card { background:var(--bg-dark2); border:1px solid var(--border); border-radius:var(--radius-lg); overflow:hidden; margin-bottom:20px; }
    .uw-card-head { display:flex; align-items:center; gap:10px; padding:16px 20px; border-bottom:1px solid var(--border); }
    .uw-card-icon { width:32px; height:32px; border-radius:9px; display:grid; place-items:center; font-size:15px; background:var(--accent-dim); color:var(--accent); }
    .uw-card-title { font-size:14px; font-weight:600; color:var(--text); }
    .uw-card-subtitle { font-size:12px; color:var(--text-muted); margin-top:1px; }
    .uw-card-body { padding:20px; }
    .uw-field { display:flex; flex-direction:column; gap:6px; margin-bottom:18px; }
    .uw-field:last-child { margin-bottom:0; }
    .uw-field label { font-size:12px; font-weight:600; color:var(--text-muted); text-transform:uppercase; letter-spacing:.6px; }
    .uw-required { color:var(--danger); margin-left:3px; }
    .uw-field input, .uw-field textarea { background:var(--bg-dark3); border:1px solid var(--border); border-radius:10px; color:var(--text); padding:10px 14px; font-size:13px; font-family:inherit; outline:none; width:100%; transition:border .2s,box-shadow .2s; }
    .uw-field textarea { resize:vertical; min-height:90px; }
    .uw-field input::placeholder, .uw-field textarea::placeholder { color:var(--text-muted); }
    .uw-field input:focus, .uw-field textarea:focus { border-color:var(--accent); box-shadow:0 0 0 3px var(--accent-dim); }
    .field-validation-error { font-size:11px; color:var(--danger); }
    .input-validation-error { border-color:rgba(255,82,89,.5) !important; box-shadow:0 0 0 3px rgba(255,82,89,.1) !important; }
    .uw-action-bar { display:flex; align-items:center; justify-content:flex-end; gap:10px; padding-top:20px; border-top:1px solid var(--border); margin-top:4px; }
    .uw-id-badge { display:inline-flex; align-items:center; gap:6px; background:var(--accent-dim); border:1px solid rgba(232,255,71,.2); border-radius:8px; padding:6px 12px; font-size:12px; color:var(--accent); font-weight:600; margin-bottom:20px; }
    .uw-icon-preview { width:80px; height:80px; border-radius:12px; background:var(--bg-dark3); border:1px dashed var(--border); display:grid; place-items:center; font-size:36px; margin-top:10px; }
</style>

<div class="uw-root">
    <div class="uw-breadcrumb">
        İçerik Yönetimi <span>›</span>
        <a href="/Admin/WhyUs/WhyUsList/">Neden Biz Listesi</a>
        <span>›</span> <span>Özellik Güncelle</span>
    </div>

    <div class="uw-page-header">
        <div>
            <h1 class="uw-page-title">Özellik Güncelle</h1>
            <p class="uw-page-subtitle">Mevcut "Neden Biz" özelliğini düzenleyin.</p>
        </div>
        <div style="display:flex;gap:10px;">
            <a href="/Admin/WhyUs/WhyUsList/" class="btn-outline">✕ İptal</a>
            <button type="submit" form="uwForm" class="btn-accent">💾 Güncelle</button>
        </div>
    </div>

    <div class="uw-id-badge">🔑 Özellik #@Model.WhyUsId</div>

    <form id="uwForm" asp-area="Admin" asp-controller="WhyUs" asp-action="UpdateWhyUs" method="post">
        @Html.AntiForgeryToken()
        <input type="hidden" asp-for="WhyUsId" />

        <div class="uw-card">
            <div class="uw-card-head">
                <div class="uw-card-icon">✨</div>
                <div>
                    <div class="uw-card-title">Özellik Bilgileri</div>
                    <div class="uw-card-subtitle">İkon, başlık ve açıklamayı güncelleyin</div>
                </div>
            </div>
            <div class="uw-card-body">
                <div class="uw-field">
                    <label>İkon (Emoji veya CSS sınıfı) <span class="uw-required">*</span></label>
                    <input type="text" asp-for="IconUrl" id="uwIcon" maxlength="200" oninput="uwIconPreview(this.value)" autocomplete="off" />
                    <span asp-validation-for="IconUrl" class="field-validation-error"></span>
                    <div class="uw-icon-preview" id="uwIconPreview">@Model.IconUrl</div>
                </div>

                <div class="uw-field">
                    <label>Başlık <span class="uw-required">*</span></label>
                    <input type="text" asp-for="Title" maxlength="100" autocomplete="off" />
                    <span asp-validation-for="Title" class="field-validation-error"></span>
                </div>

                <div class="uw-field">
                    <label>Açıklama <span class="uw-required">*</span></label>
                    <textarea asp-for="Description" maxlength="500"></textarea>
                    <span asp-validation-for="Description" class="field-validation-error"></span>
                </div>
            </div>
        </div>

        <div class="uw-action-bar">
            <a href="/Admin/WhyUs/WhyUsList/" class="btn-outline">✕ İptal</a>
            <button type="submit" class="btn-accent">💾 Güncelle</button>
        </div>
    </form>
</div>

<script>
    function uwIconPreview(val) {
        document.getElementById('uwIconPreview').textContent = val || '?';
    }
</script>

@{ await Html.RenderPartialAsync("_ValidationScriptsPartial"); }
